#ifndef NODE_H
#define NODE_H

using namespace std;

class binary_heap {
public:
  node(node *a, node *b);
  node(char a, int b);
      ~node();

private:
    char c;
    int f;
    node *l, *r;
    
    friend class heap;
};

#endif
